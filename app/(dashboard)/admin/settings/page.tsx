import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/CardClientWrapper';

export default function AdminSettingsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>管理者設定ページ（ダミー）</CardTitle>
      </CardHeader>
      <CardContent>
        ここに管理者用の設定機能を実装してください。
      </CardContent>
    </Card>
  );
} 